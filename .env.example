# Environment Configuration Example
# Copy this file to .env and modify as needed

# Set to 'production' or 'development'
ENVIRONMENT=development

# API Base URLs
# For development:
# - Android Emulator: use http://10.0.2.2:3000/api (special IP for emulator)
# - Physical Android Device: use http://[YOUR_COMPUTER_IP]:3000/api
# - iOS Simulator: use http://localhost:3000/api
# - Physical iOS Device: use http://[YOUR_COMPUTER_IP]:3000/api
#
# Find your computer's IP address:
# Windows: Open Command Prompt → type 'ipconfig' → look for IPv4 Address
# Mac: Open Terminal → type 'ifconfig' or 'ipconfig getifaddr en0'
# Linux: Open Terminal → type 'ip addr show' or 'hostname -I'
BASE_URL_LOCAL=http://localhost:3000/api
BASE_URL_PRODUCTION=https://devoverflow-backend.onrender.com/api